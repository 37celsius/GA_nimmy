CREATE DATABASE nimmy;

CREATE TABLE namedatas
(
	id SERIAL4 PRIMARY KEY,
	name VARCHAR(50) NOT NULL,
	gender VARCHAR(50),
	total INTEGER,
	min_age INTEGER,
	max_age INTEGER,
	latest_charge VARCHAR(100),
	mugshot VARCHAR(200)
);













